# Workspace Rules

- Before pushing any code, always run the CI checks locally. This includes formatting (`dart format .`), analysis (`dart analyze --fatal-infos .`), and running tests (`dart test` and `flutter test`).
