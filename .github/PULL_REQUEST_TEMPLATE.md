**What does this change and why?**

**Package(s) touched**
<!-- pulse_dev / pulse_dev_flutter / pulse_http / pulse_dio / examples / docs -->

**Checklist**
- [ ] `melos run format:check` passes
- [ ] `melos run analyze` and `melos run analyze:flutter` pass
- [ ] `melos run test` and `melos run test:flutter` pass
- [ ] `CHANGELOG.md` updated for every package whose public behavior changed
- [ ] `README.md` updated if the public API or usage changed
- [ ] No public API broken without a corresponding major version bump

**Related issue(s)**
