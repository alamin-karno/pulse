name: pulse_dev_flutter
description: >-
  Flutter integration for the Pulse Developer Intelligence SDK.
  Provides automatic error capture via FlutterError.onError and
  runZonedGuarded, Flutter context collection, and the developer-facing
  Pulse static API.
version: 0.1.0
repository: https://github.com/pulse-dart/pulse
homepage: https://github.com/pulse-dart/pulse
issue_tracker: https://github.com/pulse-dart/pulse/issues

topics:
  - monitoring
  - error-reporting
  - observability
  - analytics
  - sdk
  - flutter

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  meta: ^1.9.0
  pulse_dev: ^0.1.0

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
