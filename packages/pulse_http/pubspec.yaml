name: pulse_http
description: package:http integration for the Pulse SDK. Automatically captures network requests and sends them to Pulse.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.3.0

dependencies:
  http: ^1.2.0
  pulse_dev:
    path: ../pulse_dev

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
